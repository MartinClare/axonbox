services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: axonbox
    ports:
      - "5432:5432"
    volumes:
      - axonbox_pg:/var/lib/postgresql/data

volumes:
  axonbox_pg:
